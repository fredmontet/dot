# Template for rel/anthropic/env.zsh (which is gitignored via `**/env.zsh`).
# Copy this to env.zsh and fill in your real key, or put the export in ~/.localrc.
export ANTHROPIC_API_KEY="sk-ant-api03-REPLACE_ME"
